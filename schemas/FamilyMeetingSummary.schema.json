{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FamilyMeetingSummary",
  "type": "object",
  "properties": {
    "meetingId": {
      "type": "string",
      "description": "Eindeutige Kennung des Protokolls"
    },
    "date": {
      "type": "string",
      "format": "date",
      "description": "Datum der Besprechung im Format YYYY-MM-DD"
    },
    "participants": {
      "type": "array",
      "description": "Liste der Teilnehmer*innen",
      "items": {
        "type": "string"
      }
    },
    "agendaItems": {
      "type": "array",
      "description": "Tagesordnungspunkte der Besprechung",
      "items": {
        "type": "string"
      }
    },
    "decisions": {
      "type": "array",
      "description": "Getroffene Beschlüsse",
      "items": {
        "type": "string"
      }
    },
    "nextSteps": {
      "type": "array",
      "description": "Vereinbarte nächste Schritte",
      "items": {
        "type": "string"
      }
    },
    "notes": {
      "type": "string",
      "description": "Zusätzliche Anmerkungen oder Beobachtungen",
      "nullable": true
    },
    "attachments": {
      "type": "array",
      "description": "Dateianhänge zum Protokoll",
      "items": {
        "type": "object",
        "properties": {
          "fileName": {
            "type": "string"
          },
          "fileUrl": {
            "type": "string",
            "format": "uri"
          }
        },
        "required": ["fileName", "fileUrl"]
      }
    }
  },
  "required": ["meetingId", "date", "participants", "agendaItems", "decisions", "nextSteps"]
}
